import UpperNavBar from "./upper-nav";

const NavBar = () => {
  return (
    <nav className="sticky top-1 left-0 z-999 flex flex-col">
      <UpperNavBar />
    </nav>
  );
};

export default NavBar;
